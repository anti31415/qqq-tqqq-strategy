# 清洗后的字幕稿

说明：以下内容基于你提供的字幕手动清洗而成，主要修正了明显错词、乱码、重复句和不通顺表述；时间戳沿用原字幕，方便和视频对照。

## 00:00

你看到封面点进来，心里肯定在想：不用选股，不用每天盯盘，就靠 `QQQ` 和 `TQQQ` 这两只 ETF，普通散户真的能够赚到 `100 万美金` 吗？

如果你就是冲着这个实操方法来的，那今天你点对了。这个视频不会讲什么“15 年前 all in 比特币”这种普通人无法复制的暴富故事，而是直接给出一套普通散户可以照着执行的“百万美金滚雪球系统”。

在这里你会得到一张清晰的路线图：

- 怎么从 `2 万美金` 起步，稳定收租
- 怎么在 `10 万美金` 之后加速利润
- 怎么在 `100 万美金` 之后，搭建让你安心退休的持续现金流

先声明一下：本视频仅为个人经验与心得分享，不构成任何投资建议。

## 01:03

这套方法的核心，就是彻底放弃选股，只使用两只资产：

- `QQQ`：代表纳指科技核心资产
- `TQQQ`：`QQQ` 的三倍杠杆版本

后面拼的不是选股眼光，而是系统和纪律。

作者把这套方法定义为“指数 + 期权”的双轨交易系统。它的目标不是每天盯盘，而是每周花一两个小时，靠流程化执行去积累资本。

第一阶段的目标，是把 `2 万美金` 用“极其枯燥但稳定”的方式，复利滚动到 `10 万美金`。

第一阶段只做一件事：在 `TQQQ` 上卖出现金担保看跌期权（`Cash Secured Put`, `CSP`）。

## 02:09

为什么第一阶段选 `TQQQ` 做 `Sell Put`？

- `TQQQ` 自带三倍杠杆，波动率高，期权卖方能吃到更高权利金
- 直接持有 `TQQQ` 正股，在震荡市里会有明显的波动损耗
- 直接买 `TQQQ LEAPS`，等于在三倍杠杆上再叠一层杠杆，普通人很难承受回撤

所以作者认为：`TQQQ` 最适合做“卖 Put 收租”。

作者举的例子是：

- `TQQQ` 现价约 `49.17`
- 卖出 `33` 天后到期、行权价 `45` 的 Put
- 立刻收取约 `214 美元` 权利金

## 03:04

这一单里，作者重点强调两个数字：

1. 盈亏平衡点  
收到约 `2.1 美元/股` 权利金后，真实持仓成本从 `45` 降到 `42.9`。  
也就是说，即使 `TQQQ` 从 `49` 跌到 `42.9`，跌幅约 `13%`，这一单仍然不亏。

2. 抵押回报率  
最大风险窗口约 `4290 美元`，收到约 `210 美元` 权利金，`33` 天回报约 `4.9%`。  
作者认为，正常情况下月度回报大约在 `3% - 6%` 之间浮动，但要以实际波动率为准。

对于 `2 万美金` 级别的小账户，作者的建议是：

- 起步阶段可以适当提高期权仓位比例
- 但第一阶段的执行上，仍然尽量简单
- 只卖这一张合约
- 剩下的钱全部留作现金后备

## 04:08

第一阶段的目标是完成 `10` 轮完整循环。

“完整循环”包括两种情况：

- Put 到期归零，白拿权利金
- Put 被行权后接到股票，再通过卖 `Covered Call` 把仓位滚出去，完成闭环

如果大跌导致 Put 被行权，不要恐慌。作者把这看成是 `Wheel Strategy` 的下半场：

- 你以折扣成本接到 `100` 股 `TQQQ`
- 这时候不能被动死扛
- 接到股票的第一件事，就是立刻反手卖出 `Covered Call`

## 05:10

作者继续举例：

- 假设被行权后，真实成本约 `42.9`
- `TQQQ` 跌到 `40` 左右
- 立刻卖一张 `45` 行权价的 `Covered Call`
- 再收约 `180 美元` 权利金

之后会有两种结果：

1. `TQQQ` 反弹到 `45` 以上，股票被 call 走  
这一轮总收益包括：
   - 最初卖 Put 的权利金
   - 正股从真实成本到 `45` 的价差
   - 卖 Covered Call 的权利金

2. `TQQQ` 没涨回 `45`，Call 到期作废  
你继续收租，持仓成本进一步下降，下个月继续卖 Call，直到最后被 Call 走

这就是作者理解的“轮子策略”：

- 卖 Put 收租
- 被行权后接股票
- 卖 Covered Call 继续收租
- 股票被 Call 走后，再回到卖 Put

## 06:12

作者说，开仓只看两个最基础的指标：

- `200 日均线`
- `RSI`

但有个关键点：

- 虽然交易标的是 `TQQQ`
- 技术指标全部看 `QQQ`

理由是：`QQQ` 的技术面更稳定，更适合用来判断大方向；真正的执行放在高波动率的 `TQQQ` 上，去赚权利金。

作者给出开仓“三部曲”：

1. `QQQ` 站在 `200 日均线` 上方  
   说明大趋势偏多。

2. `QQQ RSI` 跌破 `50`  
   说明短期出现一次比较健康的洗盘回调。

3. 当天 `QQQ` 收阴线  
   不要在阳线追高去卖 Put，要等市场恐慌、隐含波动率抬升的时候开仓。

## 07:16

作者把最重要的风险分界点定义为 `QQQ 的 200 日均线`：

- 如果 `QQQ` 还在 `200 日均线` 上方，你的 Put 被行权了，那通常仍是“黄金坑”
- 可以相对放心地卖 Covered Call，等反弹解套

但如果 `QQQ` 有效跌破 `200 日均线`：

- 这是牛熊分界线
- 若此时 `Sell Put` 被行权，绝对不能用杠杆 ETF 死扛
- 要么及时止损保命
- 要么卖非常贴近、甚至价内的 Covered Call，优先防守

## 08:20

作者反复强调：从 `2 万` 到 `10 万`，不能只靠交易收益，还要靠持续入金。

他的口径是：

- 只靠每月 `3% - 6%` 的复利
- `2 万本金` 两年内大概最多滚到 `5 - 7 万`
- 剩下的缺口，要靠每月从工资里持续投入 `1000 - 2000 美元`

也就是说，起步阶段必须“劳动收入 + 投资收入”双线并进。

当账户突破 `10 万美金` 后，策略必须升级，因为如果把 `10 万` 全压在 `TQQQ Sell Put` 上，遇到黑天鹅回撤会非常痛苦。

## 09:26

从 `10 万 -> 100 万`，作者引入第二条腿：`QQQ LEAPS Call`。

重点是：

- 买的是 `QQQ LEAPS`
- 不是 `TQQQ LEAPS`

原因是：

- `TQQQ` 本身已经三倍杠杆
- 再叠加 `LEAPS`，杠杆会失控
- `QQQ + LEAPS` 才是作者认为“刚刚好”的加速器

具体口径：

- 到期日约 `2 年`
- `Delta` 约 `0.8`
- 深度价内（`Deep ITM`）Call

作者把它视为“带杠杆的正股替代品”。

## 10:27

作者同时提醒：

- `LEAPS` 是利润加速器，不是主仓
- 大跌时亏损会被放大
- 所以不能把所有利润继续加码到 `LEAPS`

多出来的利润，更应该持续买入纳指底仓。

对于长期底仓，作者更倾向于：

- 若考虑长期持有，优先 `QQQM`
- 因为费用率更低、分红略高，更适合长期复利

## 11:30

作者给出一套仓位公式：`年龄 + 20` 法则。

例子：

- 如果你 `30 岁`
- 那么把总资金的 `50%` 放进 `QQQM` 正股底仓

另外再留出：

- `10%` 纯现金仓位  
  平时不动，只在极端恐慌、`VIX >= 30` 时加仓 `QQQM`，或者额外做 `Sell Put`

- 剩下 `40%` 作为期权资金池

在期权资金池内部再做 `80 / 20` 细分：

- `80%` 给 `TQQQ Wheel`  
  占总资金约 `32%`

- `20%` 给 `QQQ LEAPS`  
  占总资金约 `8%`

作者最终归纳的配比是：

- `50%`：`QQQM` 底仓
- `10%`：现金
- `32%`：`TQQQ Wheel`
- `8%`：`QQQ LEAPS`

## 12:32

作者用 `2022` 年做压力测试说明：

- 纳指全年跌约 `33%`
- `TQQQ` 最大跌幅接近 `79%`

如果满仓死扛 `TQQQ`，账户会非常惨。

但按他的组合逻辑：

- `50%` 是 `QQQM` 底仓
- `32%` 是轮子资金
- `10%` 现金可以在恐慌时加仓

他认为，总账户最大回撤大约会落在 `25% - 35%` 区间。  
核心目标不是“永远不亏”，而是“亏了也死不掉”。

## 13:34

对于凑不出 `2 万美金` 启动资金的人，作者的建议很直接：

- 先别急着研究期权
- 先提升收入能力
- 开源、节流、存下第一桶金

因为本金太小时，即便年化回报看起来不错，绝对收益也有限。  
这 `2 万美金`，在作者眼里，是进入资本游戏的一张最低门票。

## 14:37

作者认为，`100 万` 之后真正要解决的问题，不是资产数字，而是退休后的持续现金流。

他的思路是：

- `50% - 60%` 的 `QQQM + 现金`，作为家族底仓
- `8%` 左右的 `LEAPS` 负责在牛市中继续提供超额弹性
- `32%` 左右的 `TQQQ Wheel` 负责制造每个月的现金流

这样就不需要为了花钱而卖掉底层长期资产。

## 15:42

作者还补充：

- 定投底仓不一定只能是 `QQQM`
- 也可以加入 `VOO` 或其他优质大盘蓝筹

随着年龄增加，正股底仓占比也应该自然提高：

- `40 岁`：约 `60%`
- `50 岁`：约 `70%`
- 上限可到 `80%`

即使期权仓位占比下降，绝对金额仍可能继续增长，现金流未必会变少。

## 16:47

作者最后做了一个完整总结：

- 先靠打工存下 `2 万` 的门票
- 用 `TQQQ Sell Put` 做起步阶段滚雪球
- 每个月持续投入新资金
- 到 `10 万` 后引入 `QQQ LEAPS`
- 再用“年龄 + 20”法则逐步建立长期底仓
- 等资金做大后，靠稳定的仓位配置来养老

作者强调：慢就是快，用几年的纪律换后半生的自由。

## 17:49

最后一段主要是作者介绍自己的公开账户挑战、后续视频计划和社群：

- 他正在把一个 `40 万美金` 的公开账户，按这套方法做向 `100 万` 的挑战
- 后续会继续讲“年龄 + 20”仓位管理系统
- 也欢迎观众加入他的 `Discord VIP` 频道交流期权策略
