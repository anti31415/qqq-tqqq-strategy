# 策略总结

## 1. 一句话概括

这不是一个单独的交易信号，而是一条分阶段的资本增长路线：

- 用 `QQQ` 判断趋势和回调
- 用 `TQQQ` 做高波动率轮子策略来制造现金流
- 用 `QQQ/QQQM` 底仓承接长期复利
- 用 `QQQ LEAPS` 提供牛市弹性

## 2. 资产分工

### `QQQ`

- 只做“信号源”
- 用来判断趋势、均线位置、RSI 和回调强弱

### `TQQQ`

- 只做“轮子执行载体”
- 利用其高波动率卖 Put、卖 Covered Call 收租

### `QQQ LEAPS`

- 只做“利润加速器”
- 作者明确反对用 `TQQQ LEAPS`

### `QQQM`

- 只做“长期底仓”
- 追求低费率、长期复利，不拿来频繁做期权

## 3. 阶段一：`2万 -> 10万`

### 目标

- 用 `2 万美金` 起步
- 叠加每月新增储蓄
- 先把资金规模滚到 `10 万`

### 交易动作

- 标的：`TQQQ`
- 策略：卖 `Cash Secured Put`
- 节奏：作者口播举例是 `33 DTE`

### 入场条件

作者给出的三步过滤条件是：

1. `QQQ > 200 日均线`
2. `QQQ RSI(14) < 50`
3. 当天 `QQQ` 收阴线

### 交易逻辑

- 在大趋势向上时，只等一次“健康回调”去卖 Put
- 不在阳线追高时开仓
- 目的是利用市场恐慌抬高隐含波动率，拿更厚的权利金

### 被行权后的动作

- 如果 `QQQ` 仍在 `200 日均线` 上方：
  - 接股票
  - 立刻卖 `Covered Call`
  - 争取通过反弹把仓位滚出去

- 如果 `QQQ` 已经有效跌破 `200 日均线`：
  - 作者明确不建议死扛杠杆 ETF
  - 应优先止损，或者卖非常贴近、甚至价内的 Call 做强防守

## 4. 阶段二：`10万 -> 100万`

### 目标

- 不再只靠单一轮子策略
- 组合里同时拥有：
  - 稳定底仓
  - 每月现金流
  - 牛市利润加速器

### 标准配比

以作者给出的 `30 岁` 例子为准：

- `50%`：`QQQM` 长期底仓
- `10%`：现金
- `32%`：`TQQQ Wheel`
- `8%`：`QQQ LEAPS`

### `QQQ LEAPS` 口径

- 到期日约 `2 年`
- `Delta` 约 `0.8`
- 深度价内 Call

### 组合目的

- `QQQM`：保稳定
- `TQQQ Wheel`：保现金流
- `QQQ LEAPS`：保爆发力

## 5. 阶段三：`100万+`

### 目标

- 把账户从“继续追高收益”转向“持续现金流 + 可退休性”
- 尽量不卖底层长期资产
- 用期权现金流覆盖生活

### 年龄驱动的仓位迁移

作者建议随年龄增长，逐步提高正股底仓占比：

- `30 岁`：`50%`
- `40 岁`：`60%`
- `50 岁`：`70%`
- 上限大约 `80%`

## 6. 视频里最关键的风险观

### 不是追求“永远不亏”

作者真正追求的是：

- 先活下来
- 回撤时别被一次性打死
- 留住现金和底仓，等下一轮反弹回来

### 对杠杆的态度

- `TQQQ` 可以拿来卖 Put、卖 Call，因为要赚波动率
- 但不应该长期无脑死扛
- `LEAPS` 只能放在 `QQQ` 上，不能叠到 `TQQQ` 上

## 7. 我在回测里如何把口播规则转成“可执行规则”

视频里有些地方说得很清楚，有些地方只给了方向，没有给精确参数。  
为了能回测，我采用了以下近似落地方式：

### 明确采用的视频规则

- `QQQ > 200 日均线`
- `RSI(14) < 50`
- 当天收阴
- `TQQQ` 做 `Sell Put`
- 被行权后在多头趋势里卖 `Covered Call`
- 跌破 `200 日均线` 后不死扛杠杆 ETF
- `10 万` 后组合配比用 `50 / 10 / 32 / 8`
- `QQQ LEAPS` 用 `2 年、Delta 0.8、深度价内`

### 必须补充的建模假设

- Put 到期日采用 `33` 个自然日  
  理由：视频举例直接用了 `33 天`

- Put 行权价采用“现价下方约 `8%`，四舍五入到整数”  
  理由：视频示例里 `49.17 -> 45` 大致就是这个比例

- Covered Call 默认卖在“原 Put 行权价”附近  
  理由：视频例子里被行权后也是围绕原先的 `45` 卖 Call

- 若跌破 `200 日均线` 后被行权，回测里采用“直接止损”  
  理由：视频原话给了两条路：
  - 要么及时止损
  - 要么卖极近甚至价内 Call 防守  
  为了保持规则单一，回测里选了更保守、更明确的第一种

- `QQQM` 用 `QQQ` 作为长历史代理  
  理由：`QQQM` 上市时间较晚，无法覆盖更长样本

## 8. 我认为最值得注意的现实含义

### 阶段一其实不是“暴利阶段”

从视频原意看，阶段一更像：

- 用简单轮子练执行
- 让账户别乱来
- 靠工资持续入金把雪球滚到 `10 万`

它更像“启动器”，不是主要盈利引擎。

### 真正的收益弹性来自阶段二

一旦引入：

- `QQQ/QQQM` 底仓
- `QQQ LEAPS`

组合的长期上行弹性才会明显打开。  
但同时，组合的波动和回撤也会显著上升，尤其是 `LEAPS`。
