# 指标公式与自动执行流程

## 1. 使用的指标

### 1.1 `SMA200`

定义：

`SMA200 = 最近 200 个交易日收盘价的平均值`

公式：

```text
SMA200(t) = (Close[t] + Close[t-1] + ... + Close[t-199]) / 200
```

用途：

- 判断大趋势是否向上
- 条件：`QQQ 收盘价 > SMA200`

### 1.2 `RSI(14)`

定义：

- 先计算每一天的涨跌额
- 把上涨额记为 `Gain`
- 把下跌额绝对值记为 `Loss`
- 用 14 日平滑平均得到 `AvgGain` 和 `AvgLoss`

公式：

```text
RS = AvgGain / AvgLoss
RSI = 100 - 100 / (1 + RS)
```

初始 14 日：

```text
AvgGain = 14 天上涨额之和 / 14
AvgLoss = 14 天下跌额之和 / 14
```

后续滚动更新：

```text
AvgGain(new) = (AvgGain(prev) * 13 + CurrentGain) / 14
AvgLoss(new) = (AvgLoss(prev) * 13 + CurrentLoss) / 14
```

用途：

- 判断回调是否足够深
- 条件：`RSI(14) < 50`

### 1.3 当天是否收阴

定义：

```text
DownDay = 今日收盘价 < 昨日收盘价
```

用途：

- 只在出现回调、情绪偏弱的日子考虑卖 Put

## 2. 当前交易决策规则

### 2.1 开新 Put

同时满足：

1. `QQQ > SMA200`
2. `RSI(14) < 50`
3. `今日 QQQ 收阴`
4. 当前没有未平的期权仓位
5. 当前没有需要处理的 `TQQQ` 正股仓位

则动作：

- 卖出 `TQQQ` 的 `cash-secured put`

### 2.2 卖 Covered Call

当满足：

1. 当前持有 `TQQQ` 正股
2. 数量 `>= 100`
3. 当前没有未平的期权仓位

则动作：

- 卖出对应数量的 `covered call`

### 2.3 其他情况

- 不做交易
- 记录日志
- 等待下一次定时检查

## 3. 当前合约筛选规则

### Put

- 标的：`TQQQ`
- 到期目标：约 `33` 天
- 行权价目标：`现价 * 0.92`
- 从候选链中选最接近目标行权价的 Put

### Covered Call

- 标的：`TQQQ`
- 到期目标：约 `33` 天
- 行权价目标：不低于 `平均持仓成本` 和 `当前现价` 的较大者

## 4. 自动执行流程

1. 用 `Alpaca API` 拉取：
   - 账户
   - 持仓
   - `QQQ` 日线
   - `TQQQ` 日线
   - `TQQQ` 期权链与快照

2. 计算：
   - `SMA200`
   - `RSI(14)`
   - 当天是否收阴

3. 生成动作计划：
   - `sell_cash_secured_put`
   - `sell_covered_call`
   - `hold`

4. 如果允许下单：
   - 提交 paper order

5. 把整次结果写入日志：
   - 时间
   - 信号值
   - 动作
   - 候选合约
   - 订单预览
   - 是否真正下单
   - 返回的订单结果 / 错误
